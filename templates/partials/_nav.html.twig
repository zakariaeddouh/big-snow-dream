<div class="list-group">
    {% if app.request.get('_route') == 'user_edit' %}
        <a href="{{ path('user_edit', {id : app.user.id}) }}" class="list-group-item list-group-item-action active" aria-current="true">
            Modifier mes informations
        </a>
        <a href="{{ path('user_edit_password', {id : app.user.id}) }}" class="list-group-item list-group-item-action">
            Modifier mon mot de passe
        </a>
    {% elseif app.request.get('_route') == 'user_edit_password' %}
        <a href="{{ path('user_edit', {id : app.user.id}) }}" class="list-group-item list-group-item-action">
            Modifier mes informations
        </a>
        <a href="{{ path('user_edit_password', {id : app.user.id}) }}" class="list-group-item list-group-item-action active" aria-current="true">
            Modifier mon mot de passe
        </a>
    {% endif %}
</div>